pipeline {
    agent any 

stages {

    stage("build")
    {
        steps{

            sh 'cd backend'
            sh 'echo "should print auth,gateway,and pom.xml!"'
            sh 'ls -l'
        }
    }

    stage("test")
    {
        steps {
            echo "hi from jenkins during test"
        }
    }

    stage("docker-deploy")
    {
        steps {
            echo "hi from jenkin during docker deploy"
        }
    }
}

    post {
        always {

        }
        success {

        }
        failure {

        }
    }
}