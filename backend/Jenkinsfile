pipeline {
    agent any 

stages {

    stage("build")
    {
        steps{
            sh 'mvn -f ./backend/gateway/ clean install'
            sh 'sudo docker build -t gateway_service ./backend/gateway/'
        }
    }

    stage("test")
    {
        steps {
            echo "hi from jenkins during test"
        }
    }

    stage("docker-deploy")
    {
        steps {
            echo "hi from jenkin during docker deploy"
        }
    }
}
}